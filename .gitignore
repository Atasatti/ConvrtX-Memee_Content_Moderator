# ---- Secrets ----
.env
.env.local
.env.*.local
!.env.example

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
env/
venv/

# ---- ML weights (downloaded separately, see backend/README.md) ----
*.h5

# ---- Local runtime database ----
database/history/*.sqlite3
database/history/*.sqlite3-*

# ---- Node / Next.js ----
node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# ---- Editor / OS ----
.DS_Store
.idea/
*.swp

# ---- Test fixtures ----
# Sample uploads used to exercise the analyzers. Includes explicit imagery for
# the NSFW model, so this must never reach a public remote.
# Unanchored on purpose: these match wherever the folder is moved to.
Data for testing apis/
Images_for_testing/
Videos_For_testing/
Audio_for_testing/
ocr_testing_images/

# ---- Demo video ----
# Large binaries: raw screencast frames, ffmpeg intermediates, and the rendered
# film. Distributed directly rather than through the repo.
video/
video by codex/
