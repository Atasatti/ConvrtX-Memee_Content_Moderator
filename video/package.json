{
  "name": "aegis-video",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Builds the Aegis product demo video from the real running app.",
  "scripts": {
    "record": "node record-demo.mjs",
    "compose": "node build-video.mjs",
    "build": "node record-demo.mjs && node build-video.mjs"
  },
  "dependencies": {
    "playwright": "^1.61.1",
    "sharp": "^0.34.4"
  }
}
